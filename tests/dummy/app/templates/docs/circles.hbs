<div class="row">
  <div class="col-md-7 order-2 order-md-1">
    <section>
      <h5>Creating circles</h5>

      <p>Circles are pretty much markers with a radius. Use the <tt>circle</tt> component to create a circle.</p>

      <CodeSnippet @name="basic-circles.hbs" />

      <DocTip @badgeText="Test">
        Modify the radius and fill color of the circle.
      </DocTip>

      <div class="form-group">
          <label class="col-form-label" for="radius-input">Radius</label>
          <Input @id="radius-input" @name="radius" class="form-control" @type="number" @value={{radius}} @step="100" @min="100" @max="1500" />

          <label class="col-form-label" for="fill-color-input">Fill color (hex)</label>
          <Input @id="fill-color-input" @name="fillColor" @type="color" class="form-control" @value={{fillColor}} />
      </div>
    </section>

    <p>{{nextPage.text}}</p>
    {{link-to-next nextPage}}
  </div>
  <div class="col-md-5 order-1 order-md-2 sticky-top sticky-map">
    <GMap
      @lat={{london.lat}}
      @lng={{london.lng}}
      @zoom={{14}}
      @styles={{primaryMapStyle}}
      @classNames="ember-google-map-responsive" as |g|>
      <g.circle
        @lat="51.507568"
        @lng="-0.127762"
        @radius={{radius}}
        @fillColor={{fillColor}}
        @fillOpacity={{0.3}}
        @strokeColor="darkgreen"
        @strokeOpacity={{0.5}}
        @strokeWeight={{1}} />
    </GMap>
  </div>
</div>
